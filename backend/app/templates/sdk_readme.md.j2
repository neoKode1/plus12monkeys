# {{ project_name }}

> SDK wrapper for [{{ repo_name }}]({{ repo_url }}) — auto-generated by [+12 Monkeys](https://plus12monkeys.com).

## Installation

{% if primary_language == "typescript" or primary_language == "javascript" %}
```bash
npm install
npm run build
```

## Usage

```typescript
import { {{ repo_name_safe | title }}Client } from './sdk';

const client = new {{ repo_name_safe | title }}Client();

// List all source files
const files = client.listSourceFiles();
console.log(files);

// Read a specific file
const content = client.readFile('README.md');

// Run a command in the repo
const output = client.exec('npm test');
```
{% else %}
```bash
pip install -e .
```

## Usage

```python
from sdk import {{ repo_name_safe | title }}Client

client = {{ repo_name_safe | title }}Client()

# List all modules
modules = client.list_modules()
print(modules)

# Read a specific file
content = client.read_file("README.md")

# Import and use a module
mod = client.import_module("my_module")
```
{% endif %}

## What's Included

| File | Description |
|------|-------------|
{% if primary_language == "typescript" or primary_language == "javascript" %}
| `sdk.ts` | TypeScript SDK client |
| `package.json` | npm package configuration |
| `tsconfig.json` | TypeScript compiler config |
{% else %}
| `sdk.py` | Python SDK client |
| `setup.py` | Package installation config |
| `requirements.txt` | Python dependencies |
{% endif %}
| `.env.example` | Environment variable template |
| `README.md` | This file |

## Entry Points

The following entry points were detected in the source repository:

{% for ep in entry_points %}
- `{{ ep }}`
{% endfor %}

## About

{{ repo_description }}

Generated by [+12 Monkeys](https://plus12monkeys.com) — AI Agent & SDK Builder by DeepTech AI.

